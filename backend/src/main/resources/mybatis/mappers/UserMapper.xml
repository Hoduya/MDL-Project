<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aiden.board.mapper.UserMapper">
    <select id="findByUserId" resultType="UserDto">
        SELECT USER_ID, EMAIL, PASSWORD, NAME, ROLE, REG_DATE
        FROM TB_USER
        WHERE USER_ID = #{userId}
    </select>
    
    <select id="findByUserEmail" resultType="UserDto">
        SELECT USER_ID, EMAIL, PASSWORD, NAME, ROLE, REG_DATE
        FROM TB_USER
        WHERE EMAIL = #{email}
    </select>
    
    <select id="findByUserName" resultType="UserDto">
        SELECT USER_ID, EMAIL, PASSWORD, NAME, ROLE, REG_DATE
        FROM TB_USER
        WHERE NAME = #{name}
    </select>
    
    <insert id="save" parameterType="UserDto">
        INSERT INTO TB_USER (EMAIL, PASSWORD, NAME, REG_DATE)
        VALUES ( #{email}, #{password}, #{name}, #{regDate} )
    </insert>
</mapper>