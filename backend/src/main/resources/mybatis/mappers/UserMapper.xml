<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aiden.board.mapper.UserMapper">
    <select id="findByUserId" resultType="UserDto">
        SELECT id, password, name, role, regDate
        FROM user
        WHERE id = #{id}
    </select>
    
    <insert id="save" parameterType="UserDto">
        INSERT INTO user (id, password, name, regDate)
        VALUES ( #{id}, #{password}, #{name}, #{regDate} )
    </insert>
</mapper>